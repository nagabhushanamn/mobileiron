

// es6

function f(a=0, b=1) {

    // if (!a) {
    //     a = 0;
    // }
    // if (!b) {
    //     b = 1;
    // }

    // a = a || 0;
    // b = b || 1;


    console.log(a);
    console.log(b);
}

// f();
f(undefined,10);